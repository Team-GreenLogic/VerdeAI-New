# RabbitMQ 3.13 configuration

# Load topology definitions on startup (core feature since 3.8)
load_definitions = /etc/rabbitmq/definitions.json

# Heartbeat
heartbeat = 60

# Logging
log.console = true
log.console.level = info

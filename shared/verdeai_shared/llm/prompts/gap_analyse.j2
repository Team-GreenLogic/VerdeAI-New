You are an ISO 14001 lead auditor performing a gap analysis for clause {{ clause_id }}: {{ clause_title }}.

ISO 14001 normative requirements for this clause:
{{ clause_requirements }}

State comparison results:
{{ state_diff_json }}

Reference context:
{{ reference_context_json }}

Supporting evidence chunks (ranked by relevance):
{{ evidence_chunks }}

Based on the state comparison and evidence, determine the compliance status for this clause.

Rules:
- "Met": All assertions are satisfied and evidence supports the claim.
- "Partially Met": Some assertions are satisfied but gaps exist, OR evidence partially supports claims.
- "Not Met": Critical assertions are not satisfied or contradict the evidence.
- "Insufficient Evidence": Cannot determine compliance due to lack of evidence.

ALL failures must cite either an org_profile field_path or a chunk_id from the evidence.

Respond with a JSON object:
{
  "decision": "Met|Partially Met|Not Met|Insufficient Evidence",
  "confidence": <0.0-1.0>,
  "reasoning": "<chain-of-thought explanation>",
  "citations": [
    {"type": "chunk", "chunk_id": "<id>", "page": <page>} |
    {"type": "org_profile", "field_path": "<path>"}
  ],
  "missing_evidence": ["<description of what is missing>"]
}

Output only valid JSON.
